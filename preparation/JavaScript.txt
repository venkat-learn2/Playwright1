1.Javascript array methods 
===========================
    > push() - add elements to the end of the array
    > pop() - removes the last element 
    > shift() - removes the first element 
    > unshift() - add elements to starting of the array 
    > map() - creating new array by transforming elements()
    > filter() - returns elements that match a condition 
    > reduce() - reduces array to a single value 
    > forEach() - loops through array no returns - undefined
    > find() - returns first matching element 
    > findIndex() - returns the index of the first match 
    > includes() - check if the value exists 
    > indexOf() - returns index of element if not returns -1
    > slice() - return portion of the array (no changes in original)
    > splice() - add removes/elements(modifies the original)
    > concat() - Merge Arrays 
    > join() - converts arrays to string 
    > sort() - to make ascending order 
    > reverse() - reverses the arrray 
    > every() - check if all the elements satisfy condition - returns true or false
    > some() - checks if atleast one satisfies the condition - returns true or false
=====================================================================================================
2.Javascript String methods 
    >includes()
    > slice()
    > split()
    > replace()
    > replaceAll()
    > trim()
    > IndexOf()
    > StartsWith()
    > EndsWith()
    > toUpperCase()
    > toLowercase()
===================================================================================================================
3.What is promise ? 
    > A promise is an Object that represents the result of an async operation
    > 3 states - Pending , fulfilled(Resolved), Rejected 
    
    const myPromise = new Promise((resolve,reject)=>{
        let success = true;
        if(success){
            resolve("task completed")
        }else{
            reject("task failed")
        }
    })

    myPromise.then((result)=>{
        console.log(result)
    }).catch((err)=>{
        console.log(err)
    })

    > we use .then to handle the success and use the catch for the error

    > for easier syntax we can use async/await

    async function handlePromise(){
        try{
            const result = true;
            console.log(result)
        }catch(error){
            console.log(error)
        }
    }

    > await - Wait until the promise is resolved before moving to next line.

    > async/await is a cleaner way to handle promises. 
    The async keyword makes a function return a promise, 
    and await pauses execution until the promise is resolved.
===================================================================================================================
4. What is Recursion ?

    > A function calling itself until a condition met 

===================================================================================================================
5.Exception Handling in JS ?

    > Handle runtime error without crashing the program 

    try{
        // code that may throw error 
    }catch{
        // handle the error 
    }finally{
        // always runs 
    }
=============================================================================================
6.let , var , const difference
    > var is function scoped we can access the variable throughout the function .
    > let is block level scoped cannot access outside of the block {}.
    > var redeclaration allowed var a = 10; var a = 20 ; no error 
    > let redeclaration not allowed let a = 10;let a = 20;throw error 
    > both are hoister for var it is undefined , for let it is reference error
    > const does not allow reassignment const a =10,a = 30 ; throws error TypeError: Assignment to constant variable
    > const also block level scope like let
    > const does not allow declaration also 
========================================================================================================================================
7.What is Hoisting ? 
    > Hoisting means JavaScript moves variable and function declarations to the top of their scope before execution.
    > console.log(a);
        var a = 10;  undefined 
========================================================================================================================================
8.What is Closure?
    > A closure is when a function remembers variables from its outer scope even after the outer function has finished execution.
    > 
========================================================================================================================================
9.What is the Temporal Dead Zone (TDZ)?
    > The time between variable hoisting and declaration where a variable cannot be accessed.
    > Applies to let and const.
    > console.log(a); let a = 10; --> result = reference error 
========================================================================================================================================
10.What is the difference between == and ===?
    > == Compare the values only
    > === Compare the values and respective data types 
========================================================================================================================================
11.What is the difference between null and undefined?
    > by default or intentionally value should be empty null
    > variable declared value not assigned is undefined
========================================================================================================================================
12.What is the difference between map, filter, and reduce?
    > map - transform array 
    > filter - select elemets 
    > reduce - accumulate the results 
    > arr.map(x => x*2)
      arr.filter(x => x>5)
      arr.reduce((a,b)=>a+b)
========================================================================================================================================
13.What is this in JavaScript?
    > this refers to the object that is currently executing the function.
    > const user = {
        name: "Venkat",
        greet(){
        console.log(this.name);
        }   
    }   
========================================================================================================================================

